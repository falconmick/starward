// inside of hear you would follow similar patterns as apollo/types. Pull in your Type definitions and resolvers and then bundle them
// followed by creating a module of all of your bundles, root querie and root mutation.
// then addonnApolloModules would import this module so that Apollo GraphQL can add your new Type Definitions
